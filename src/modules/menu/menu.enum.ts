export enum EMenuStoreType {
  ShopeeFood = 1,
  GrabFood = 2,
}

export enum EMenuActionButton {
  CloseConfirm = 'close-confirm',
  Order = 'order',
}

export enum EMenuCloseConfirmAction {
  Yes = 'yes',
  No = 'no',
}

export enum EMenuFormName {
  DishIds = 'dish-ids',
  Note = 'note',
}
